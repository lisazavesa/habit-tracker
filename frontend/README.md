# Habit Tracker - Frontend

Полнофункциональное веб-приложение для отслеживания привычек, построенное на React, TypeScript, Mantine UI и Redux Toolkit.

## 🚀 Возможности

- 👤 **Аутентификация и авторизация** - регистрация, вход, выход
- 📝 **Управление привычками** - создание, редактирование, удаление привычек
- 📊 **Отслеживание логов** - запись выполнения привычек с датой и заметками
- 🎨 **Адаптивный дизайн** - работает на мобильных и десктопных устройствах
- 🌈 **Mantine UI компоненты** - красивый и интуитивный интерфейс
- ⚡ **Redux Toolkit** - управление состоянием приложения

## 📦 Стек технологий

- **React 18** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик
- **Mantine UI 7** - компоненты UI
- **Redux Toolkit** - управление состоянием
- **React Router DOM** - маршрутизация
- **Axios** - HTTP клиент
- **js-cookie** - управление cookies

## 🛠️ Установка

```bash
# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Собрать для production
npm run build

# Предпросмотр production сборки
npm run preview

# Проверка типов
npm run type-check

# ESLint проверка
npm run lint
```

## 📋 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
VITE_API_URL=http://localhost:3000
```

## 📁 Структура проекта

```
src/
├── api/              # API слой (auth, habits)
├── components/       # React компоненты
│  ├── AuthPage.tsx  # Страница входа/регистрации
│  ├── HabitsList.tsx # Список привычек
│  ├── HabitModal.tsx # Модал создания/редактирования привычки
│  ├── HabitLogsModal.tsx # Модал с логами привычки
│  └── Header.tsx    # Шапка приложения
├── hooks/           # Custom React hooks
│  └── redux.ts      # Redux типизированные hooks
├── pages/           # Страницы приложения
│  ├── HomePage.tsx  # Главная страница
│  └── SettingsPage.tsx # Страница настроек
├── store/           # Redux store
│  ├── authSlice.ts  # Auth редьюсер
│  ├── habitsSlice.ts # Habits редьюсер
│  ├── statsSlice.ts # Stats редьюсер
│  └── index.ts      # Конфигурация store
├── types/           # TypeScript типы
├── utils/           # Утилиты
├── App.tsx          # Root компонент
└── main.tsx         # Точка входа
```

## 🎯 Функциональность

### Аутентификация

- Регистрация нового пользователя
- Вход в систему
- Сохранение JWT токена в cookies
- Автоматическое восстановление сессии

### Управление привычками

- Создание новых привычек с параметрами:
  - Название и описание
  - Целевое количество
  - Единица измерения
  - Частота (ежедневно, еженедельно, ежемесячно)
  - Цвет для визуализации
- Редактирование существующих привычек
- Удаление привычек
- Просмотр всех привычек в виде карточек

### Отслеживание прогресса

- Запись выполнения привычки с количеством
- Добавление заметок к записям
- Просмотр полной истории логов привычки
- Удаление отдельных логов

## 🔗 API Интеграция

Приложение интегрируется с бекенд API:

```
POST   /auth/register      - Регистрация
POST   /auth/login         - Вход
GET    /auth/profile       - Получить профиль

GET    /habits             - Получить все привычки
POST   /habits             - Создать привычку
PATCH  /habits/:id         - Обновить привычку
DELETE /habits/:id         - Удалить привычку

GET    /habits/:id/logs           - Получить логи привычки
POST   /habits/:id/logs           - Создать/обновить лог
DELETE /habits/:id/logs/:logId    - Удалить лог
```

## 🎨 Адаптивный дизайн

Приложение полностью адаптивно и поддерживает:

- 📱 Мобильные устройства (320px+)
- 📱 Планшеты (768px+)
- 🖥️ Десктопы (1024px+)

Используются CSS Grid, Flexbox и Media Queries для оптимальной верстки.

## 🔒 Безопасность

- JWT токены хранятся в httpOnly cookies
- Автоматические заголовки Authorization для защищенных запросов
- Типизирование TypeScript для предотвращения ошибок

## 🚀 Запуск приложения

```bash
# Убедитесь что бекенд запущен на порту 3000
npm run dev
```

Приложение откроется на `http://localhost:5173`

## 📝 Лицензия

MIT
