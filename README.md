# Habit Tracker

Проект — простой трекер привычек с бэкендом на NestJS и фронтендом на Vite + React.

**Требования**:

- **Node.js**: версия 18+ (рекомендуется)
- **npm** (или yarn/pnpm)
- **Docker** и **docker-compose** (для запуска PostgreSQL)
- `npx` (обычно поставляется с npm)

**Коротко о структуре**:

- `backend/` — NestJS API, Prisma, миграции
- `frontend/` — Vite + React приложение
- `legacy/` — старые версии/архив

## 📦 Стек технологий

### Frontend

- **React 18**
- **Redux Toolkit**
- **TypeScript**
- **Mantine UI**
- **React Router DOM**
- **Axios**

### Backend

- **Nest.js**
- **TypeScript**
- **bcrypt**
- **PrismaORM**
- **Class Validator**

**Запуск проекта с нуля (локально)**

1. Клонируйте репозиторий и перейдите в корень:

```bash
git clone <repo-url>
cd habit-tracker
```

2. Запустите PostgreSQL через Docker Compose (из папки `backend`):

```bash
cd backend
docker-compose up -d
```

Контейнер поднимает PostgreSQL 16 с параметрами по умолчанию:

- порт: `5432` на хосте
- пользователь/пароль: `postgres`/`postgres`
- БД: `habits_db`

3. Установите зависимости для бэкенда и фронтенда (в корневых папках):

```bash
# в одном терминале
cd backend
npm install

# в другом терминале
cd frontend
npm install
```

4. Настройте переменные окружения

- В `backend/.env` должны быть (есть пример):

```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/habits_db?schema=public
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=1d
```

- Для фронтенда создайте файл `.env` или `.env.local` в папке `frontend` со строкой:

```env
VITE_API_URL=http://localhost:3000
```

5. Сгенерируйте Prisma client и примените миграции

```bash
cd backend
npx prisma generate
npx prisma migrate deploy
```

Если вы разрабатываете и хотите выполнить миграцию в режиме разработки, можно использовать:

```bash
npx prisma migrate dev
```

6. Запустите бэкенд (dev режим):

```bash
cd backend
npm run start:dev
```

API будет доступно по умолчанию: `http://localhost:3000`.
Swagger (документация) доступна по: `http://localhost:3000/api`

7. Запустите фронтенд:

```bash
cd frontend
npm run dev
```

Фронтенд по умолчанию будет на `http://localhost:5173` (или другом свободном порту).
